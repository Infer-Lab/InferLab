# InferLab Measurement SDK

Internal generated protocol models and runtime helpers shared by InferLab's
release-owned Eval and Bench runners. This package carries the InferLab product
version and is delivered only inside the versioned measurement toolchain. It is
not an independently published workspace dependency.
